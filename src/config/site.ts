import type { AnimationName } from '../infrastructure/animations/registry';

/**
 * Single switch controlling which background animation is active.
 * See src/infrastructure/animations/registry.ts for available names.
 */
export const ACTIVE_BACKGROUND_ANIMATION: AnimationName = 'blackhole';
